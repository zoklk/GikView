replicaCount: 2

image:
  repository: emqx/emqx
  tag: "5.8.6"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  ports:
    mqtts: 8883
    dashboard: 18083
    ekka: 4370

nodePortService:
  nodePorts:
    mqtts: 31884

tls:
  secretName: emqx-server-tls
  mountPath: /etc/emqx/tls

acl:
  configMapName: emqx-acl
  mountPath: /etc/emqx/acl

certificate:
  issuerName: step-cluster-issuer
  duration: 2160h
  renewBefore: 720h
  clusterDomain: cluster.local
  ipAddresses: []

emqxConfig:
  EMQX_CLUSTER__DISCOVERY_STRATEGY: "dns"
  EMQX_CLUSTER__DNS__RECORD_TYPE: "srv"
  EMQX_LISTENERS__TCP__DEFAULT__ENABLED: "false"
  EMQX_LISTENERS__SSL__DEFAULT__BIND: "0.0.0.0:8883"
  EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__CERTFILE: "/etc/emqx/tls/tls.crt"
  EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__KEYFILE: "/etc/emqx/tls/tls.key"
  EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__CACERTFILE: "/etc/emqx/tls/ca.crt"
  EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__VERIFY: "verify_peer"
  EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__FAIL_IF_NO_PEER_CERT: "true"
  EMQX_MQTT__PEER_CERT_AS_USERNAME: "cn"
  EMQX_AUTHORIZATION__NO_MATCH: "deny"
  EMQX_AUTHORIZATION__SOURCES: '[{type = file, enable = true, path = "/etc/emqx/acl/acl.conf"}]'
  # Dashboard/management HTTP listener also serves the Prometheus pull endpoint
  # (/api/v5/prometheus/stats); EMQX OSS can't split it onto its own listener. Binding to
  # loopback (이전 127.0.0.1) RST 시킨 in-cluster scrape → emqx 메트릭 0. 0.0.0.0 로 열되
  # 외부 노출은 없음 — NodePort 는 mqtts(31884)만, 18083 은 ClusterIP/headless 클러스터 내부 전용.
  EMQX_DASHBOARD__LISTENERS__HTTP__BIND: "0.0.0.0:18083"

resources:
  requests:
    cpu: "200m"
    memory: "384Mi"
  limits:
    cpu: "500m"
    memory: "384Mi"

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  fsGroup: 10001
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault
