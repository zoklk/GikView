KSV-0125

# InfluxDB 3 Core image runs as uid/gid 1500 by default.
# Phase spec (context/phases/storage.md) sets `chown 1500:1500` on the host PV path.
# KSV-0020 / KSV-0021 prefer uid/gid > 10000 — not applicable to this upstream image.
AVD-KSV-0020
AVD-KSV-0021

# cert-manager webhook dynamic-serving cert RBAC — single named secret
# (cert-manager-webhook-ca) get/list/watch/update/create. Upstream jetstack/cert-manager
# vendored chart; not our code. KSV-0113 prefers no secret management at namespace scope.
KSV-0041
KSV-0048
KSV-0049
KSV-0056
KSV-0113
KSV-0114

# telegraf/templates/configmap.yaml — Telegraf TOML config embedded in ConfigMap.
# token value is env-var placeholder ${INFLUXDB_TOKEN}; real secret injected via
# Deployment env from influxdb-admin-token Secret. Flagged "key" variants
# (tls_key path, json_time_key, processor key/result_key) are TOML field names.
KSV-0109
KSV-01010